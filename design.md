# design.md — 앱 디자인

> 작업하며 지속 업데이트하는 문서입니다.

## 1. 디자인 톤
- 가족 친화적, 따뜻하고 심플. 사진/영상이 주인공이므로 UI는 최소·중립.
- 라이트/다크 모드 지원(시스템 설정 따름). 둥근 모서리, 부드러운 그림자.
- 색: 중립 배경(흰/짙은 회색) + 포인트 컬러 1종(따뜻한 앰버/코랄 계열).
- 폰트: 시스템 기본 산세리프(가독성·로딩 속도).

## 2. 화면 구성
### 2.1 로그인 (`/login`)
- 앱 이름/로고, 이메일 입력 1개, "매직링크 받기" 버튼.
- 전송 후 "메일함을 확인하세요" 안내.
- 초대되지 않은 이메일은 접근 불가 안내.

### 2.2 갤러리 (홈 `/`)
- 상단 헤더: 앱 이름, 업로드 버튼(관리자), 멤버 메뉴/로그아웃.
- 본문: **썸네일 그리드**(반응형 2~5열). 동영상은 재생 아이콘 + 길이 배지.
- 정렬: 촬영일 최신순 기본. 무한 스크롤/페이지네이션.
- 항목 클릭 → 상세 뷰어.

### 2.3 상세 뷰어 (`/media/[id]`)
- 전체화면 모달/페이지. 이미지: 원본 표시(핀치 줌). 동영상: `<video>` 스트리밍(탐색 가능).
- 하단/우측: 원본 다운로드, 메타(파일명·크기·촬영일), (관리자) 삭제.
- 좌우 스와이프/화살표로 이전·다음 이동.

### 2.4 업로드 (`/upload` 또는 모달, 관리자)
- 드래그앤드롭 + 파일 선택(다중, 이미지/동영상).
- 파일별 진행률 바, 대용량은 멀티파트 진행 표시, 실패 재시도.
- 완료 시 갤러리로 반영.

### 2.5 멤버 관리 (`/members`, 관리자)
- 멤버 목록(이메일·역할·상태). 이메일로 초대 추가. 멤버 제거.

## 3. 컴포넌트 목록
- `Header` / `NavMenu`
- `MediaGrid`, `MediaCard`(썸네일+배지)
- `MediaViewer`(이미지/비디오 분기), `MediaMeta`
- `UploadDropzone`, `UploadProgressItem`
- `MemberList`, `InviteForm`
- `MagicLinkForm`
- `EmptyState`, `Spinner`, `Toast`

## 4. 반응형 / PWA
- 모바일 우선. 그리드 열 수: 모바일 2 / 태블릿 3~4 / 데스크톱 5.
- PWA: `app/manifest.ts`(이름·아이콘·테마색·standalone), 서비스워커로 앱 셸 캐싱(미디어 원본은 캐시 제외 — 용량).
- 홈 화면 추가 안내(iOS Safari/안드로이드).

## 5. 상태/로딩 UX
- 스켈레톤 썸네일, 업로드 중 백그라운드 진행, 오류 토스트.
- 빈 갤러리: "첫 사진을 올려보세요" 안내 + 업로드 버튼.

## 6. 접근성
- 이미지 alt(파일명/촬영일), 키보드 내비게이션, 충분한 대비.
